#gallery
  .album-title
    a
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      display: inline-block;
      max-width: 95%;

  a
    background: none;

  .card
    &:hover
      background-color: #1e1e1e !important;

      a
        color: $color-accent;