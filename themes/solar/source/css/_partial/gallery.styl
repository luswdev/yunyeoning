#gallery
  p
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    display: inline-block;
    max-width: 100%;

  a
    background: none;
    &:hover
      color: $color-accent